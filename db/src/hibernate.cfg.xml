<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>
        <!--  HSQL connection -->
        <property name="connection.driver_class">org.hsqldb.jdbcDriver</property>
        <property name="connection.url">jdbc:hsqldb:file:hsqldb/storage</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">2</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.HSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup
       <property name="hbm2ddl.auto">create</property>-->

        <!--<property name="transaction.factory_class">org.hibernate.transaction.JDBCTransactionFactory</property>-->
        <!--  thread is the short name for
          org.hibernate.context.ThreadLocalSessionContext
          and let Hibernate bind the session automatically to the thread
        -->
        <property name="current_session_context_class">thread</property>
        <!-- this will show us all sql statements -->
        <property name="hibernate.show_sql">true</property>
        <!-- this will create the database tables for us -->
        <!--<property name="hibernate.hbm2ddl.auto">create</property>-->
        <!--<mapping resource="de/laliluna/example/Honey.hbm.xml" />-->
        <mapping class="satteliteExplorer.db.entities.Constraint"/>
        <mapping class="satteliteExplorer.db.entities.DataCenter"/>
        <mapping class="satteliteExplorer.db.entities.Effectiveness"/>
        <mapping class="satteliteExplorer.db.entities.Equipment"/>
        <mapping class="satteliteExplorer.db.entities.EquipmentType"/>
        <mapping class="satteliteExplorer.db.entities.Orbit"/>
        <mapping class="satteliteExplorer.db.entities.Region"/>
        <mapping class="satteliteExplorer.db.entities.Role"/>
        <mapping class="satteliteExplorer.db.entities.Sat"/>
        <mapping class="satteliteExplorer.db.entities.Task"/>
        <mapping class="satteliteExplorer.db.entities.User"/>
    </session-factory>

</hibernate-configuration>
